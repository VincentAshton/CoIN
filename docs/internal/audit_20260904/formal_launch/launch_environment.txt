CoIN+Replay ratio=0.1 正式 sweep 启动环境（2026-09-04 03:29 CST）
不含任何密码/token/SSH 凭据。

实例：ebcloud GPU（ssh-cn-huabei1.ebcloud.com:30267，root）
仓库：/root/data/coin/project @ 17cfa66f009bd1fd1f5d360307f97d4249bf2c5c（干净）
Conda env：/root/data/coin/conda_envs/coin（python 3.10.21）
  torch 2.0.1+cu118 / transformers 4.32.0 / deepspeed 0.14.0 / accelerate 0.21.0 / peft 0.4.0
env.sh：缓存目录（PIP_CACHE_DIR/XDG_CACHE_HOME/TORCH_EXTENSIONS_DIR/TRITON_CACHE_DIR/
  CUDA_CACHE_PATH/HF_HOME/TRANSFORMERS_CACHE/TMPDIR 等全部指向 /root/data/coin）+ CUDA_HOME
  + nvcc/conda PATH + HF_ENDPOINT=hf-mirror
数据根：/root/data/coin（coinssd 持久卷，791G 可用；系统盘 / 30G 用 109M）

正式环境变量：
  REPLAY_ACCUM=1        （方案 D：replay 段 accum=1 全 ratio/round 统一）
  ENFORCE_MIN_STEPS=1   （门禁生效）
  GPUS=0,1,2,3  CUDA_VISIBLE_DEVICES=0,1,2,3
  PREFLIGHT_ARGS='--layout-map {"ImageNet":"ImageNet_withlabel"}'
  ALLOW_SINGLE_STEP_REPLAY：未设置（=0，方案 D 不需要）

命令：bash scripts/CoIN_Replay/run_sweep.sh 0.1（唯一；0.01 严禁启动）

GPU：4×A100-SXM4-80GB 空闲，NVLink NV12 全互联，NUMA 1
硬件检查：pip check OK；无 torchrun/deepspeed/train_mem/run_sweep 残留
